const books = ["calculas", "physics", "chemistry", "math", "javascript"];
if (books.includes("javascript"))
  console.log(
    "the book is here click the link to download pdf file of this book"
  );
else {
  console.log("you are doing a waste attempt feel so sad for that?");
}
